# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is committed on purpose: cPanel git deployment copies the
# pre-built bundle to public_html (shared hosting cannot run `npm build`).
dist-ssr
*.local

# Local working artifacts (contact sheets, scratch output) — not for hosting
tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
